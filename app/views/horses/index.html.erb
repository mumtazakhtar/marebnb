<div class="cards-container">
  <div class="cards">
  <% @horses.each do |horse|%>
    <div class="card-horse">
      <% if horse.photo.key %>
      <%= cl_image_tag horse.photo.key, height: 300, width: 400, crop: :fill %>
      <% end %>
    <div class="card-horse-infos">
      <h2><%=link_to horse.name, horse_path(horse) %></h2>
      <div class="info">
        <p>Age: <%= horse.age %></p>
        <p>Breed: <%= horse.breed %></p>
        <p>Price: <%= horse.price %></p>
      </div>
        <%# <p>For a full description, click in the link!</p> %>
        <%# <%= horse.description %>
    </div>
  </div>
  <% end %>
  </div>
</div>

<div class="footer">
  <div class="footer-links">
    <a href="#"><i class="fab fa-github"></i></a>
    <a href="#"><i class="fab fa-instagram"></i></a>
    <a href="#"><i class="fab fa-facebook"></i></a>
    <a href="#"><i class="fab fa-twitter"></i></a>
    <a href="#"><i class="fab fa-linkedin"></i></a>
  </div>
  <div class="footer-copyright">
    <h5>Follow us at our social media channels</h5>
  </div>
</div>

    <%# <img src="http://kitt.lewagon.com/placeholder/cities/shanghai" style="width: 100%" class="rounded-top" /> %>
    <%# <%= cl_image_tag("THE_IMAGE_ID_FROM_LIBRARY",
      width: 200, height: 150, crop: :fill) %>
